import java.io.File;

// this file contains 3 lines of code
public abstract class TestFile1 {
    /**
     * count the number of lines in a file
     */
    abstract int countLines(File inFile); // not the real signature!
}